export enum RulerFactoryOption {
  Start = "START", // used before active page reaches the middle of the ruler
  End = "END", // used when we get closer to the last page
  Default = "DEFAULT" // default behavior
}
